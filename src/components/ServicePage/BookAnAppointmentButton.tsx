'use client';

import CalenderIcon from '@/icons/CalenderIcon';
import RTONButton from '../RTONButton';

export default function BookAnAppointmentButton() {
  return <RTONButton onClick={() => {}} text="Book an appointment" icon={<CalenderIcon />} />;
}
