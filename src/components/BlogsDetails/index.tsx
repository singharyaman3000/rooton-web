'use client';

import { IBlogDetails } from '@/app/services/apiService/blogDetailAPI';
import { useEffect } from 'react';

const BlogDetails = ({ details }: { details: IBlogDetails }) => {
  console.log('ssssssss', details);

  return <div></div>;
};

export default BlogDetails;
